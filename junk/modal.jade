#myModal.modal.hide.fade(tabindex='-1', role='dialog')
  .modal-header
    button.close(type='button', data-dismiss='modal') ×
    h3#myModalLabel Modal header
    form#modalform(action="/save/nameage")
      .modal-body
        .row-fluid
          .span6
            h4 Create NameAge
            label Name:
            input.span12(type='text', name='name')
            label Age:
            input.span12(type='text', name='age')
      .modal-footer
        button.btn(type='button', data-dismiss='modal') Close
        button.btn.btn-primary(type='submit') Save
script
  $(function(){
    initForm('#myModal', {
       age: "required"
     });
  });
